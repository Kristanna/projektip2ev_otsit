<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Otsija</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="header">
        <a href="default" class="logo"><img src="images/logo.png"></a>
        <div class="header-right">
            <a class="Home" href="home">Kodu</a>
            <a href="filler">Sisestaja</a>
            <a href="finder">Otsija</a>
        </div>
    </div>

    <div>
        <!-- Otsi nuppu vajatades tehakse POST /finder endpointi, name="productName" pannakse POSTI'ga kaasa -->
        <form action="/finder" method="POST">

            <label for="productName">Toote nimi:</label>
            <input type="text" id="productName" name="productName" required>
            <br><br>


            <button type="submit">Otsi</button>
        </form>
    </div>
    <table>
        <thead>
            <tr>
                <th>Pilt</th>
                <th>Nimi</th>
                <th>Hind</th>
                <th>Kogus</th>
                <th>Asukoht poes/laos</th>
            </tr>
        </thead>
        <tbody id="data-output">
            <% for (let product of found) { %>
            <tr>
                <td> <img src='<%= product.image %>'> </td>
                <td><%= product.name %></td>
                <td><%= product.price %></td>
                <td><%= product.inventory %></td>
                <td><%= product.productCode %></td>
            </tr>
            <% } %>

        </tbody>
    </table>
</body>
</html>